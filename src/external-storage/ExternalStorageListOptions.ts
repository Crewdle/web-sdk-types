/**
 * Options for the list function on an external storage.
 * @category External Storage
 */
export interface IExternalStorageListOptions {
  /**
   * List folders recursively.
   */
  recursive?: boolean;
}
