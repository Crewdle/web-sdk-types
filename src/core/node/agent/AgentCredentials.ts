/**
 * The agent credentials interface.
 * @category Core
 */
export interface IAgentCredentials {
  /**
   * The agent ID.
   */
  id: string;

  /**
   * The agent group ID.
   */
  groupId: string;
}
