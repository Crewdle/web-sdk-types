/**
 * The node type enum.
 * @category Core
 */
export enum NodeType {
  /**
   * The user node type.
   */
  User = 'user',

  /**
   * The agent node type.
   */
  Agent = 'agent',

  /**
   * The cloudlet node type.
   */
  Cloudlet = 'cloudlet',
}

// TODO - Review naming for agent and cloudlet
